{
  "name": "lovenow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"No tests for static site\" && exit 0",
    "build": "echo \"Static site - nothing to build\" && exit 0",
    "start": "npx serve ."
  }
}
